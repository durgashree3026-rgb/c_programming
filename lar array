#include<stdio.h>
int main()
{
    int n;
    scanf("%d",&n);
    int arr[n];
    int lar = arr[0];
    
    //read the array
    for(int i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }
    
    //traversal array
    for(int i=0;i<n;i++)
    {
        if(arr[0]<arr[i])
        {
            lar = arr[i];
        }
        
    }
    printf("%d",lar);
    return 0;
}
