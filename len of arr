#include<stdio.h>
int main()
{
    char str[50];
    int count=0;
    fgets(str,sizeof(str),stdin);
    int len;
    //traversal
    for(int i=0;str[i]!='\0';i++)
    {
        count++;
    }
    printf("%d",count);
    return 0;
}
